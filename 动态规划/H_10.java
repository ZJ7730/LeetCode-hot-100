/**
 * @program: suanfa
 * @ClassName: H_10
 * @description: 10. 正则表达式匹配
 * @author: zhoujie07
 * @create: 2026-05-26
 **/
public class H_10 {
    /**
     * 待实现思路，参考代码随想录动规五部曲：
     * 1. dp数组含义：dp[i][j] 表示 s 的前 i 个字符 s[0..i) 是否能被 p 的前 j 个字符 p[0..j) 匹配。
     * 2. 递推公式：
     *    - 如果 p[j - 1] 是普通字符或 '.'，看 s[i - 1] 和 p[j - 1] 是否匹配，匹配则继承 dp[i - 1][j - 1]。
     *    - 如果 p[j - 1] 是 '*'，它可以让前一个字符出现 0 次，继承 dp[i][j - 2]；
     *      如果 s[i - 1] 能匹配 p[j - 2]，也可以让前一个字符出现多次，继承 dp[i - 1][j]。
     * 3. 初始化：dp[0][0] = true；空串匹配形如 a*、a*b* 的模式时，需要初始化 dp[0][j]。
     * 4. 遍历顺序：从前往后遍历 s 和 p 的前缀。
     * 5. 解题思路：关键是把 '*' 理解为修饰它前面的字符，而不是单独匹配字符。
     */
}
