/**
 * @program: suanfa
 * @ClassName: H_64
 * @description: 64. 最小路径和
 * @author: zhoujie07
 * @create: 2026-05-26
 **/
public class H_64 {
    /**
     * 待实现思路，参考代码随想录动规五部曲：
     * 1. dp数组含义：dp[i][j] 表示从左上角走到坐标 (i, j) 的最小路径和。
     * 2. 递推公式：只能从上方或左方到达当前位置，
     *    dp[i][j] = Math.min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]。
     * 3. 初始化：dp[0][0] = grid[0][0]；第一行只能从左边累加，第一列只能从上边累加。
     * 4. 遍历顺序：从左到右、从上到下。
     * 5. 解题思路：和不同路径类似，只是路径数相加变成路径和取最小。
     */
}
