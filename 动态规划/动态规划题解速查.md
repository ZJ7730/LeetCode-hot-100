# 动态规划题解速查

## 目录

### 线性 DP

- [70. 爬楼梯](#70-爬楼梯)
- [198. 打家劫舍](#198-打家劫舍)
- [300. 最长递增子序列](#300-最长递增子序列)

### 网格 DP

- [62. 不同路径](#62-不同路径)
- [64. 最小路径和](#64-最小路径和)
- [221. 最大正方形](#221-最大正方形)

### 背包 DP

- [279. 完全平方数](#279-完全平方数)
- [416. 分割等和子集](#416-分割等和子集)
- [494. 目标和](#494-目标和)
- [322. 零钱兑换](#322-零钱兑换)

### 字符串 DP

- [139. 单词拆分](#139-单词拆分)
- [72. 编辑距离](#72-编辑距离)

### 回文区间 DP

- [5. 最长回文子串](#5-最长回文子串)
- [647. 回文子串](#647-回文子串)

## 一、线性 DP

### 70. 爬楼梯

`dp[i]` 表示到达第 `i` 阶楼梯的方法数。

```java
class Solution {
    public int climbStairs(int n) {
        if (n <= 2) {
            return n;
        }
        int[] dp = new int[n + 1];
        dp[1] = 1;
        dp[2] = 2;

        for (int i = 3; i <= n; i++) {
            dp[i] = dp[i - 2] + dp[i - 1];
        }
        return dp[n];
    }
}
```

### 198. 打家劫舍

`dp[i]` 表示考虑下标 `0...i` 的房屋时，能够偷到的最大金额。

```java
class Solution {
    public int rob(int[] nums) {
        if (nums.length == 1) {
            return nums[0];
        }
        int[] dp = new int[nums.length];
        dp[0] = nums[0];
        dp[1] = Math.max(nums[0], nums[1]);

        for (int i = 2; i < nums.length; i++) {
            dp[i] = Math.max(dp[i - 2] + nums[i], dp[i - 1]);
        }
        return dp[nums.length - 1];
    }
}
```

### 300. 最长递增子序列

`dp[i]` 表示必须以 `nums[i]` 结尾的最长严格递增子序列长度。

```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        if (nums.length <= 1) return nums.length;
        int[] dp = new int[nums.length];
        int result = 0;
        Arrays.fill(dp, 1);
        for (int i = 1; i < nums.length; i++) {
            for (int j = 0; j < i; j++) {
                if (nums[i] > nums[j]) {
                    dp[i] = Math.max(dp[j] + 1, dp[i]);
                }
            }
            if (dp[i] > result) {
                result = dp[i];
            }
        }
        return result;
    }
}
```

## 二、网格 DP

### 62. 不同路径

`dp[i][j]` 表示从左上角到达坐标 `(i,j)` 的路径数。

```java
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] dp = new int[m][n];
        for (int i = 0; i < m; i++) {
            dp[i][0] = 1;
        }
        for(int i = 0; i<n; i++) {
            dp[0][i] = 1;
        }

        for (int i = 1; i<m;i++) {
            for (int j = 1;j<n;j++) {
                dp[i][j] = dp[i-1][j] + dp[i][j-1];
            }
        }

        return dp[m-1][n-1];
    }
}
```

### 64. 最小路径和

`dp[i][j]` 表示从左上角走到坐标 `(i,j)` 的最小路径和。

```java
class Solution {
    public int minPathSum(int[][] grid) {
        int[][] dp = new int[grid.length][grid[0].length];
        dp[0][0] = grid[0][0];

        for (int i = 1; i < grid.length; i++) {
            dp[i][0] = dp[i - 1][0] + grid[i][0];
        }

        for (int i = 1; i < grid[0].length; i++) {
            dp[0][i] = dp[0][i - 1] + grid[0][i];
        }

        for (int i = 1; i < grid.length; i++) {
            for (int j = 1; j < grid[0].length; j++) {
                dp[i][j] = Math.min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j];
            }
        }

        return dp[grid.length - 1][grid[0].length - 1];
    }
}
```

### 221. 最大正方形

`dp[i][j]` 表示以 `matrix[i-1][j-1]` 为右下角的最大全 `1` 正方形边长。

```java
class Solution {
    public int maximalSquare(char[][] matrix) {
                int m = matrix.length;
        int n = matrix[0].length;

        int[][] dp = new int[m + 1][n + 1];

        int res = 0;

        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                if (matrix[i - 1][j - 1] == '1') {
                    dp[i][j] = Math.min(Math.min(dp[i - 1][j], dp[i - 1][j - 1]), dp[i][j - 1]) + 1;
                } else {
                    dp[i][j] = 0;
                }
                res = Math.max(res, dp[i][j]);
            }
        }
        return res * res;
    }
}
```

## 三、背包 DP

### 279. 完全平方数

`dp[j]` 表示凑成正整数 `j` 所需的最少完全平方数数量。

```java
class Solution {
    public int numSquares(int n) {
        int[] dp = new int[n + 1];
        dp[0] = 0;
        for (int i = 1; i <= n; i++) {
            dp[i] = Integer.MAX_VALUE;
        }

        for (int i = 1; i * i <= n; i++) {
            for (int j = i * i; j <= n; j++) {
                dp[j] = Math.min(dp[j], dp[j - i * i] + 1);
            }
        }

        return dp[n];
    }
}
```

### 416. 分割等和子集

`dp[j]` 表示容量为 `j` 的背包最多能装入的元素总和。

```java
class Solution {
    public boolean canPartition(int[] nums) {
        int target = 0;
        for (int i = 0; i < nums.length; i++) {
            target += nums[i];
        }
        if (target % 2 != 0) {
            return false;
        }
        target = target / 2;

        int[] dp = new int[target + 1];

        for (int i = 0; i < dp.length; i++) {
            dp[i] = 0;
        }

        for (int i = 0; i < nums.length; i++) {
            for (int j = target; j >= nums[i]; j--) {
                dp[j] = Math.max(dp[j], dp[j - nums[i]] + nums[i]);
            }
        }

        return dp[target] == target;
    }
}
```

### 494. 目标和

`dp[j]` 表示从已处理的数组元素中选择一些元素，凑出和 `j` 的方案数。

```java
class Solution {
    public int findTargetSumWays(int[] nums, int target) {
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        if (Math.abs(target) > sum) {
            return 0;
        }
        if ((target + sum) % 2 == 1) {
            return 0;
        }

        int bagSize = (target + sum) / 2;

        int[] dp = new int[bagSize + 1];
        dp[0] = 1;

        for (int i = 0; i < nums.length; i++) {
            for (int j = bagSize; j >= nums[i]; j--) {
                dp[j] += dp[j - nums[i]];
            }
        }

        return dp[bagSize];
    }
}
```

### 322. 零钱兑换

`dp[j]` 表示凑成金额 `j` 所需的最少硬币数量。

```java
class Solution {
    public int coinChange(int[] coins, int amount) {
        int[] dp = new int[amount + 1];
        dp[0] = 0;
        for (int i = 1; i <= amount; i++) {
            dp[i] = Integer.MAX_VALUE;
        }

        for (int i = 0; i < coins.length; i++) {
            for (int j = coins[i]; j <= amount; j++) {
                if (dp[j - coins[i]] != Integer.MAX_VALUE) {
                    dp[j] = Math.min(dp[j], dp[j - coins[i]] + 1);
                }
            }
        }
        return dp[amount] == Integer.MAX_VALUE ? -1 : dp[amount];
    }
}
```

## 四、字符串 DP

### 139. 单词拆分

`dp[i]` 表示字符串前 `i` 个字符 `s[0,i)` 能否由字典中的单词拼出。

```java
class Solution {
      public boolean wordBreak(String s, List<String> wordDict) {
          boolean[] dp = new boolean[s.length() + 1];
          dp[0] = true;

          // 先遍历背包容量，保证考虑单词的排列顺序
          for (int i = 1; i <= s.length(); i++) {
              for (String word : wordDict) {
                  int length = word.length();

                  if (i >= length
                          && dp[i - length]
                          && s.substring(i - length, i).equals(word)) {
                      dp[i] = true;
                      break;
                  }
              }
          }

          return dp[s.length()];
      }
  }
```

### 72. 编辑距离

`dp[i][j]` 表示将 `word1` 前 `i` 个字符转换成 `word2` 前 `j` 个字符所需的最少操作数。

```java
class Solution {
    public int minDistance(String word1, String word2) {
        char[] word1CharArray = word1.toCharArray();
        char[] word2CharArray = word2.toCharArray();

        int[][] dp = new int[word1CharArray.length + 1][word2CharArray.length + 1];

        for (int i = 0; i <= word1CharArray.length; i++) {
            dp[i][0] = i;
        }

        for (int j = 0; j <= word2CharArray.length; j++) {
            dp[0][j] = j;
        }

        for (int i = 1; i <= word1CharArray.length; i++) {
            for (int j = 1; j <= word2CharArray.length; j++) {
                if (word1CharArray[i - 1] == word2CharArray[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1];
                } else {
                    dp[i][j] = Math.min(Math.min(dp[i - 1][j] + 1, dp[i][j - 1] + 1), dp[i - 1][j - 1] + 1);
                }
            }
        }

        return dp[word1CharArray.length][word2CharArray.length];
    }
}
```

## 五、回文区间 DP

### 5. 最长回文子串

`dp[i][j]` 表示字符串闭区间 `s[i...j]` 是否为回文子串。

```java
class Solution {
    public String longestPalindrome(String s) {
        char[] word = s.toCharArray();
        int length = word.length;

        boolean[][] dp = new boolean[length][length];

        int start = 0;
        int len = 0;

        for (int i = length - 1; i >= 0; i--) {
            for (int j = i; j < length; j++) {
                if (word[i] == word[j]) {
                    if (j - i <= 1) {
                        dp[i][j] = true;
                    } else {
                        dp[i][j] = dp[i + 1][j - 1];
                    }

                    int currentLength = j - i + 1;
                    if (dp[i][j] && currentLength > len) {
                        start = i;
                        len = currentLength;
                    }
                }
            }
        }
        return s.substring(start, start + len);
    }
}
```

### 647. 回文子串

`dp[i][j]` 表示字符串闭区间 `s[i...j]` 是否为回文子串。

```java
class Solution {
    public int countSubstrings(String s) {
        char[] word = s.toCharArray();
        int length = word.length;

        int res = 0;

        boolean[][] dp = new boolean[length][length];

        for (int i = length - 1; i >= 0; i--) {
            for (int j = i; j < length; j++) {
                if (word[i] == word[j]) {
                    if (j - i == 0 || j - i == 1) {
                        dp[i][j] = true;
                    } else {
                        dp[i][j] = dp[i + 1][j - 1];
                    }
                    if (dp[i][j]) {
                        res++;
                    }
                }
            }
        }
        return res;
    }
}
```
