/**
 * @program: suanfa
 * @ClassName: H_32
 * @description: 32. 最长有效括号
 * @author: zhoujie07
 * @create: 2026-05-26
 **/
public class H_32 {
    /**
     * 待实现思路，参考代码随想录动规五部曲：
     * 1. dp数组含义：dp[i] 表示以下标 i 结尾的最长有效括号长度。
     * 2. 递推公式：只有 s[i] == ')' 时才可能形成有效括号。
     *    - 如果 s[i - 1] == '('，dp[i] = dp[i - 2] + 2。
     *    - 如果 s[i - 1] == ')'，需要看 i - dp[i - 1] - 1 位置是否是 '('；
     *      如果是，则 dp[i] = dp[i - 1] + 2 + dp[i - dp[i - 1] - 2]。
     * 3. 初始化：dp 默认 0。
     * 4. 遍历顺序：从左到右。
     * 5. 解题思路：dp[i] 必须强调“以 i 结尾”，遇到 ')' 时尝试和前面的 '(' 配对，并接上更早的有效段。
     */
}
